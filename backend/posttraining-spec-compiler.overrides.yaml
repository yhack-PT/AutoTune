# Compiler-owned local overrides for posttraining-spec-compiler.mjs.
# Set to `null` to fall back to the planner prompt default of 1.0 epochs.
sft_num_train_epochs: null
# Set to `null` to keep the planner-selected GPU.
sft_gpu_type: null
# Set to `null` to keep the planner-selected max_length.
sft_max_length: null
# Accepts either `org/name` or a full `https://huggingface.co/datasets/...` URL.
sft_dataset: null

show_evaluation_component: null

